# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ece_global
DB_USER=postgres
DB_PASSWORD=

# Servidor
PORT=3001
NODE_ENV=development

# JWT — generar valores seguros con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# CORS
FRONTEND_URL=http://localhost:5173

# Correo electrónico (SMTP)
# Si SMTP_HOST está vacío el sistema simulará el envío en consola (útil en desarrollo)
SMTP_HOST=smtp.tuproveedor.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu_correo@dominio.com
SMTP_PASS=tu_password_smtp
SMTP_FROM="SIRES" <noreply@ece-global.mx>
