# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ece_global
DB_USER=postgres
DB_PASSWORD=

# Servidor
PORT=3001
NODE_ENV=development

# JWT — generar valores seguros con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# CORS
FRONTEND_URL=http://localhost:5173
